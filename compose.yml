services:
  votix:
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - 5000:5000

    volumes:
      - ./data:/app/app/var
      - ./data/uploads:/app/app/static/uploads
      - ./logs:/app/app/logs

    restart: unless-stopped